spring:
  ai:
    mcp:
      client:
        enabled: true
        name: kaspel-essayist
        version: 0.1.0
        request-timeout: 30s
        type: SYNC
        stdio:
          connections:
            docker-mcp:
              command: docker
              args:
                - mcp
                - gateway
                - run
